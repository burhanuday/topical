import React from "react";
import { Block, Text } from "../../components/ui";

const Chat = ({ navigation }) => {
  return (
    <Block>
      <Text>Chat</Text>
    </Block>
  );
};

export default Chat;
